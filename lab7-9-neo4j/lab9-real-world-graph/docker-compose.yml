version: '3.8'
services:
  neo4j:
    image: neo4j:5.15
    container_name: neo4j_server
    ports:
      - "7474:7474" # HTTP port
      - "7687:7687" # Bolt port
    environment:
      - NEO4J_AUTH=neo4j/password
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
